#ifndef MAIN_H
#define MAIN_H
int _putchar(char c);
int _printf(const char *format, ...);
/**int _printf2(const char *fmt, ...);**/
/**typedef struct fmt
{
	char c;
	char *get_arg(va_list);
} fmt_t;**/
#endif
